
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<a href="@Url.Action("Login","Account")">Login</a><a href="@Url.Action("Register","Account")">Register</a>
<div class="alert alert-success">
    <p>Hello @User.Identity.Name! Start chating...</p>
</div>
<div ng-controller="AccountController as accountCtr">
    <button ng-click="accountCtr.getListUser()">Get list member</button>
    <ul id="list-user">
        <li ng-repeat="member in accountCtr.listMemeber">{{member.UserName}}</li>
    </ul>
</div>
@*@section scripts {
    <!--Script references. -->
    <!--The jQuery library is required and is referenced by default in _Layout.cshtml. -->
    <!--Reference the SignalR library. -->
    <script src="~/Scripts/jquery.signalR-2.2.0.min.js"></script>
    <!--Reference the autogenerated SignalR hub script. -->
    <script src="~/signalr/hubs"></script>
    <!--SignalR script to update the chat page and send messages.-->
    <script>
$(function () {
    // Reference the auto-generated proxy for the hub.
    var chat = $.connection.serviceHub;
    // Create a function that the hub can call back to display messages.
    chat.client.getListMember = function (listUser) {
        // Add the message to the page.
        $('#list-user').empty();
        $.each(listUser, function (key, value) {
            $('#list-user').append('<li><strong>' + value.UserName+ '</strong></li>');
        });
    };
    $.connection.hub.start().done(function () {
        $('#getList').click(function () {
            // Call the Send method on the hub.
            chat.server.listUser();
        });
    });
});
</script>
    }*@
